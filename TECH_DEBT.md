# Technical Debt

Items spotted during development that should be fixed but don't block current work.
**Rule**: Add here instead of fixing mid-feature. Review monthly, fix at least 1 per cycle.

## Active

| ID | Description | Area | Priority | Added |
|----|-------------|------|----------|-------|
| TD-002 | axe-core `color-contrast` rule disabled in tests — CSS custom properties not resolvable in jsdom. Programmatic contrast test added as mitigation (`contrast.test.ts`). | testing | 🟢 Low | 2026-03-23 |
| TD-003 | `e2e/visual/harness.tsx` only has showcases for ~46/84 components — the full a11y sweep (`a11y-full.spec.ts`) skips the other ~38 (they render the "not found" fallback). The observatory's "color-contrast ×61 DS components" was this false positive, not real defects. Add showcases for the remaining components so the sweep gives real coverage, then gate CI on it. | testing/a11y | 🟠 High | 2026-06-04 |
| TD-005 | ~43 rare-variant arbitrary values remain after the TD-004 migration (`placeholder:`/`peer-*`/`group-hover`/`before:` + gradient `from`/`to`/`divide`) — no regression (still consumer-scanned) but worth finishing + adding an ESLint rule banning `*-[var(--ui-…)]` color utilities to prevent future drift. | architecture | 🟢 Low | 2026-06-25 |
| TD-008 | Preexisting lint debt at shared-config adoption: 33 problems (4 errors, 29 warnings) — mostly `consistent-type-imports` errors + `no-unused-vars` warnings in tests. Not fixed inline; CI runs `@jplannnou/eslint-config` in ratchet mode (only changed files gated), so debt burns down organically. | tooling | 🟡 Medium | 2026-07-02 |
| TD-011 | **The visual harness runs on the Vite dev server, so component CSS is JS-injected after first paint.** That unstyled window is what let TD-010's baselines get recorded broken, and `toHaveScreenshot`'s match-and-stop polling made it self-perpetuating. Regenerating the baselines removes today's symptom, but the window still exists — a future baseline can be poisoned the same way. Structural fix: serve a **built** harness (`vite build` + `vite preview`) so CSS is a blocking `<link>`, or block on the injected styles before screenshotting. | testing | 🟡 Medium | 2026-07-15 |
| TD-012 | **Google Fonts never loads in the visual harness on CI** — every baseline renders Montserrat/Quicksand as a fallback serif (visible in any `__snapshots__` PNG). Consistent, so it doesn't cause flakiness, but the snapshots don't reflect real typography and can't catch font-related regressions. It also means a CI network change would invalidate all 60 baselines at once. Fix: self-host the fonts in the harness instead of the CDN `<link>` in `e2e/visual/index.html`. | testing | 🟢 Low | 2026-07-15 |
| TD-013 | **The brand gradient's green end is short of AA for normal text.** `--ui-gradient` is `linear-gradient(135deg, #24495A, #409C35)` and `PaywallUnified` puts `text-sm font-semibold` white on it. White measures **9.65:1** on the blue end but **3.49:1** on the green end — over 1.4.11's 3:1, under AA-normal's 4.5:1. White is nonetheless the right ink (the gradient doesn't flip with the theme, and surface-dark is 1.41:1 on the blue end — see the comment at `PaywallUnified.tsx`), so the fix isn't the ink: it's darkening the gradient's green stop, or promoting the CTA text to AA-large (≥18.66px bold). **Brand decision — needs JP.** | a11y/brand | 🟡 Medium | 2026-07-15 |

## Done

| ID | Description | Fixed |
|----|-------------|-------|
| TD-001 | `--ui-text-muted` dark contrast — **bumped #828b98 → #9aa4b2 to pass WCAG AA (≥4.5:1) on dark surface.** (Earlier "accepted large-only"; promoted to a real fix when greening the a11y sweep.) | 2026-06-26 |
| TD-007 | CopyButton axe color-contrast on UA `#efefef` — **harness false-positive** (no Tailwind preflight in the e2e harness; real apps reset native button bg). Added `CopyButton` to `contrastExclusions` in `a11y.spec.ts`. | 2026-06-26 |
| TD-009 | **`text-white` on semantic backgrounds failed contrast in the dark theme.** Root cause: TD-001 tuned the dark tokens to read *as text on* the dark surface, which made them light — and therefore bad *as backgrounds under* white ink. Measured on dark: white on `primary #67C728` **2.15:1**, `success #22c55e` **2.28:1**, `error #f87171` **2.77:1**. **Fixed** with `gu-text-surface`, which flips with the theme and clears AA both ways (primary 6.35/8.71, success 5.98/6.45, error 4.93/5.86): `Pagination`, `StepIndicator`, `UploadWizard`, `BottomBar` + `FloatingActionButton` badges, plus `Checkbox` in #71 (`RadioGroup` was already correct). `PaywallUnified` deliberately keeps white — its background is the theme-invariant gradient; split out as TD-013. Was invisible to CI because axe's `color-contrast` is off (TD-002) and the sweep skips ~38 components (TD-003); pinned now by `semantic-ink.test.tsx`. | 2026-07-15 (PR #74) |
| TD-010 | **The visual gate was asserting that components look like their *broken* selves.** Two compounding faults: (1) the budget was a ratio of the **full page** — 1280×720 = 921.600 px, so `maxDiffPixelRatio: 0.01` allowed **9.216 differing px** against a Checkbox showcase of **1.024 px**, i.e. blind to any small component; (2) worse, `toHaveScreenshot` polls and stops the moment a frame **matches** the expected image, and the harness's CSS is JS-injected by the Vite dev server (TD-011) — so a baseline recorded in the unstyled window matched an early unstyled frame on every later run, forever. **Fixed** by an absolute budget (`maxDiffPixels: 100`, measured: two `--update-snapshots` runs are byte-identical across all 60, so there is no jitter band) + regenerating baselines on CI Linux, which changed **41 of 60** — `Callout` by 554.392 px (its soft backgrounds were absent), `AlertBanner` by 335.111. Proof of the blindness: #69 changed the Checkbox's border *and* added a fill, and `visual` reported SUCCESS. Together with TD-002 and TD-003, this is the full answer to how an invisible checkbox shipped and survived. | 2026-07-15 (PR #72) |
| TD-004 | (Was: harness without `@tailwindcss/vite` → colors fall back to browser defaults.) **Resolved by migrating colored arbitrary values** `*-[var(--ui-*)]` (1482 across 104 components, variant-aware) to static classes in `src/ui-classes.css`. Components now get colors from plain CSS imported per-file — independent of any consumer/harness Tailwind scan (also fixes the "invisible component" bug class + unblocks contrast testing). | 2026-06-25 (PR `ds/css-classes-and-lint`) |
| TD-006 | Card-as-button vs `CardButton` split — **keep the split.** `CardButton` (accessible native `<button>`) stays the dedicated interactive component; `Card` remains a non-interactive container, not polymorphic. | 2026-06-25 (decision) |

---
**Priority levels**: 🔴 Critical (security/data) · 🟠 High (perf/UX) · 🟡 Medium (code quality) · 🟢 Low (nice to have)
